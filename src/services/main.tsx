import React from "react";
import ReactDOM from "react-dom/client";
import AuthUpload from "pages/AuthUpload"; // Import trang AuthUpload

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
    <React.StrictMode>
        <AuthUpload />
    </React.StrictMode>
);
